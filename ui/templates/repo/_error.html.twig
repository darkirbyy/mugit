{% import 'theme/macro.html.twig' as m %}

<turbo-frame id="tb-repo-list">
  {% set message = ('repo.error.' ~ coreError.messageKey)|trans|capitalize %}
  {{ m.alert('error', 'repo-list-' ~ coreError.messageKey, message) }}
</turbo-frame>
