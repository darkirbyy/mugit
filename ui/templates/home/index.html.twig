{% extends 'base.html.twig' %}

{% set title = 'home.title'|trans %}

{% block main %}
  <h1 class="text-3xl font-bold">Hello from μGit + Tailwind CSS!</h1>Lines :<ul>
    {% for repoInfo in repoInfoList %}
      <li>{{ repoInfo.name }} - {{ repoInfo.cloneUrl }} - {{ repoInfo.humanReadableSize }}</li>
    {% endfor %}
  </ul>
{% endblock %}
