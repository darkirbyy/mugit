{% extends 'base.html.twig' %}
{% import 'theme/macro.html.twig' as m %}

{% set title = 'admin.users.title'|trans %}

{% block main %}
  <turbo-frame id="turboframe-admin-users-list" src="{{ path('admin_users_list') }}" data-controller="turboframe">
    {{ m.spinner(5, true, 'p-3 gap-3 grid-cols-1', 'h-8') }}
  </turbo-frame>
{% endblock %}
