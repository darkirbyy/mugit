{% extends 'base.html.twig' %}
{% import 'theme/macro.html.twig' as m %}

{% set title = 'error.title'|trans({ code: errorCode }) %}

{% block main %}
  <p>
    {{ ('error.message.' ~ errorKey)|trans }}
  </p>
  <p>
    {{ m.link('error.backToHome', path('home_index')) }}
  </p>
{% endblock %}
